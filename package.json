{
  "private": true,
  "scripts": {
    "clean": "rm -rf build",
    "start": "snowpack dev",
    "build": "snowpack build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "preact": "^10.4.4",
    "silmarils": "^0.2.0"
  },
  "devDependencies": {
    "snowpack": "^2.3.1"
  }
}
